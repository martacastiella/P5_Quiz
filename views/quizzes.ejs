<h1>
    Questions:
</h1>

<table>
    <% for (var i in quizzes) { %>
    <% var quiz = quizzes[i]; %>
    <tr>
        <td>
            <%= quiz.question %> : <%= quiz.answer %>        
	</td>
        
    </tr>
    <% } %>
</table>

